# WIRING

Nothing owed to any external repository or system.

This build is fully self-contained in its own repository. It is an extract: it
copies content across from a source system rather than depending on it, and it
references no path outside this repo in any shipped file. No file outside this
repository was created or modified during the build.

The source system already carries its own live record of this lane; maintaining
that record is the orchestrator's job, not this lane's, and no change to it is
proposed here.
